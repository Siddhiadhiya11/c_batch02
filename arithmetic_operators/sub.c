#include<stdio.h>
int main()
{
    int num1,num2,diff;
    printf("enter two numbers : ");
    scanf("%d%d",&num1,&num2);
    diff = num1 - num2;
    printf("The difference of two numbers is %d",diff);
    return 0;
}